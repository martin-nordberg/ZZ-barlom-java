buildDir = '../../../build/grestler-foundation/persistence/grestler-ioutilities'

dependencies {

    // Other Modules
    compile project( ':grestler-utilities' )

    // Flyway
    compile( group: 'org.flywaydb', name: 'flyway-core', version: flywayVersion )

}
