buildDir = '../../../build/barlom-server/domain/barlom-metamodel'

dependencies {

    // Other Modules
    compile project( ':barlom-utilities' )

    // JSON-P
    compile( group: 'org.glassfish', name: 'javax.json', version: jsonpVersion )

}
