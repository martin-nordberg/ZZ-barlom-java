{{! Properties tab panel of the Schema page }}

{{#with propertiesPanelFields}}

    <table class="table table-condensed" id="propertiesPanelTable">

        <thead>
        <tr>
            <th>
                Properties
            </th>
            <th></th>
        </tr>
        </thead>

        <tbody>

            {{#each fields}}

                <tr>
                    <td>
                        <label for="field-{{name}}">{{label}}:</label>
                    </td>

                    <td>

                        {{#if isText}}
                            <input id="field-{{name}}" type="text" name="{{name}}" on-change="textChanged" value="{{value}}" {{#if readOnly}}disabled{{/if}}>
                        {{/if}}

                        {{#if isRadioGroup}}
                            {{#each radioButtons:index}}
                                <label class="radio-label" for="field-{{name}}-{{index}}">
                                    <input id="field-{{name}}-{{index}}" type="radio" name="{{name}}" on-change="radioChanged" checked="{{checked}}" value="{{value}}" {{#if readOnly}}disabled{{/if}}>
                                    {{label}}
                                </label>
                            {{/each}}
                        {{/if}}

                        {{#if isDropDown}}
                            <select id="field-{{name}}" name="{{name}}" on-change="dropDownChanged" {{#if readOnly}}disabled{{/if}}>
                                {{#each selections}}
                                    <option value="{{value}}" {{#if selected}}selected{{/if}}>{{label}}</option>
                                {{/each}}
                            </select>
                        {{/if}}

                    </td>
                </tr>

            {{/each}}
        </tbody>

    </table>

{{/with}}

